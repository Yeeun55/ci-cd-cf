steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  args:
  - gcloud
  - functions
  - deploy
  - cicdcf
  - --source=./
  - --trigger-http
  - --runtime=python38
  - --entry-point=hello_world
